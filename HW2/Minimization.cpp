/*
 * To illustrate the idea of minimization, we have a Hello world program.
 * It's extremely simple, there isn't much code required to run it.
 * No privelages are required to run it because it's so simple.
 * No arrays are used so that it's impossible for a segmentation fault to occur
 */

#include <iostream>

using std::cout;
using std::endl;

int main()
{
  cout << "Hello world." << endl;

  return 0;
}
